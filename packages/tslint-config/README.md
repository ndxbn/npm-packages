My TSLint config base.
 
## Install

install packages: `npm i -D @ndxbn/tslint-config tslint tslint-config-prettier tslint-plugin-prettier`

put `tslint.json` on your project root as:

```json
{
  "extends": [
    "@ndxbn/tslint-config"
  ]
}
```
